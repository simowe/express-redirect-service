import { redirect } from './redirect'

redirect('localhost', 'http://google.com')
redirect('localhost', 'http://google.com')
redirect('localhost', 'http://google.com')
redirect('localhost', 'http://google.com')
redirect('localhost', 'http://google.com')
